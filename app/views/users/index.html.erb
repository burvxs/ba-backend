<h2>All Users</h2>
<%= link_to "Create a new user", new_user_path %>
<hr>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user.id) %>
      <%= user.admin %>
    </li>
  <% end %>
