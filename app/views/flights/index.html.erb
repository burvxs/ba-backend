<h2>All Planes</h2>
<%= link_to "Create a new flight", new_flight_path %>
<hr>
  <% @flights.each do |flight| %>
  <li>
    <%= link_to flight.flight_number, flight_path(flight.id) %>
  </li>
  <% end %>
